import { PATH as GIFS_VIEW_PATH } from 'scenes/GifViewer/constants';
import { PATH as SIGNUP_PATH } from 'scenes/SignUp/constants';
import { PATH as LOGIN_PATH } from 'scenes/LogIn/constants';

export const modalPaths = [
  GIFS_VIEW_PATH,
  SIGNUP_PATH,
  LOGIN_PATH
]

export const CLOSE_MODAL = 'services.modal.close_modal';